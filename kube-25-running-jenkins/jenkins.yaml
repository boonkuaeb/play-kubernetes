Master:
  AdminPassword: password
  resources:
    limits:
      cpu: "500m"
      memory: "1024Mi"
  serviceType: NodePort
  nodePort: 32303

persistence:
  storageClass: "managed-nfs-storage"
  size: "5Gi"

